

export default function CreateArrayList(arr) {
    function createElement(arr) {
        for (let i = 0; i < arr.length; i++) {
            return (
                <li></li>
            )
        }
    }
}